var myTuple = []; //tuple;
var array = new Array(4); // array declarion
var arr; // declare 
arr = [1, 2, 3, 4];
for (var i = 0; i < arr.length; i++) {
    console.log(arr[i]);
}
var list = [1, 3, 4, 5, "Ajay"]; // list 
